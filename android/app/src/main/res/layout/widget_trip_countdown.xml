<?xml version="1.0" encoding="utf-8"?>
<!--
  วิดเจ็ตหน้าโฮม "อีก 17 วันไปเขาช้างเผือก"

  ใช้ได้แต่ view ที่ RemoteViews รองรับ — เส้นคั่นจึงเป็น ImageView ที่ตั้งพื้นหลัง
  ไม่ใช่ <View> เปล่า ๆ ซึ่งไม่อยู่ในรายการที่รองรับ

  คอลัมน์ขวา (ยอดค้างชำระ) ถูกซ่อน/แสดงจากโค้ดตามความกว้างจริงของวิดเจ็ต —
  ดู TripCountdownWidget.isWide()
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:padding="14dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/widget_icon"
                    android:layout_width="13dp"
                    android:layout_height="13dp"
                    android:layout_marginEnd="5dp"
                    android:src="@drawable/ic_widget_mountain"
                    android:contentDescription="@string/widget_trip_countdown_name" />

                <TextView
                    android:id="@+id/widget_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:includeFontPadding="false"
                    android:textColor="#B8FFFFFF"
                    android:text="@string/widget_trip_countdown_name" />
            </LinearLayout>

            <TextView
                android:id="@+id/widget_big"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:maxLines="2"
                android:ellipsize="end"
                android:textSize="34sp"
                android:textStyle="bold"
                android:includeFontPadding="false"
                android:textColor="#10B981" />

            <TextView
                android:id="@+id/widget_caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="11sp"
                android:includeFontPadding="false"
                android:textColor="#B8FFFFFF" />

            <ProgressBar
                android:id="@+id/widget_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:layout_marginTop="8dp"
                android:max="100"
                android:progressDrawable="@drawable/widget_progress"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:maxLines="2"
                android:ellipsize="end"
                android:textSize="11sp"
                android:includeFontPadding="false"
                android:textColor="#8CFFFFFF" />
        </LinearLayout>

        <ImageView
            android:id="@+id/widget_divider"
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:background="#24FFFFFF"
            android:visibility="gone"
            android:contentDescription="@null" />

        <LinearLayout
            android:id="@+id/widget_payment_column"
            android:layout_width="104dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:id="@+id/widget_payment_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="11sp"
                android:textStyle="bold"
                android:includeFontPadding="false"
                android:textColor="#B8FFFFFF" />

            <TextView
                android:id="@+id/widget_payment_amount"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="18sp"
                android:textStyle="bold"
                android:includeFontPadding="false"
                android:textColor="#FFFFFFFF" />

            <TextView
                android:id="@+id/widget_payment_due"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:maxLines="2"
                android:ellipsize="end"
                android:textSize="11sp"
                android:includeFontPadding="false"
                android:textColor="#8CFFFFFF" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
